# False-positive suppressions for the public-repo secret scan.
# Format: path/to/file (whole file exempt) OR path/to/file:N (line N exempt).

# The secret-detector script contains the patterns it detects — self-match is expected.
scripts/check-public-repo-secrets.py
